def get_workflow_prompt(query: str) -> str:
    return f"""
# 用户输入
请判断以下 Query 是否在工作流产品的真实能力范围内：
Query: {query}

请判断：
- 当前 Query 是否在该产品真实能力范围内
- 是否超出产品能力边界
- 是否存在不合理或无法实现的诉求

# 限制
请严格按照以下 JSON 格式输出，不要包含任何其他文字：
{{
    "within_ability_scope": true/false,
    "reason": "能力边界判断说明"
}}

## 多维表格工作流说明
### 工作流概念
多维表格由飞书出品，本质是一个可扩展的业务系统，由任意数量的数据表、字段和工作流组成。工作流是一套自动化引擎：通过配置触发条件，当条件满足时，系统自动执行预设动作，减少重复操作并提升效率。
### 工作流结构
一个工作流可抽象为由一个触发器节点 + 多个操作节点组成的树状结构：
- 触发器节点：定义启动条件；
- 操作节点：包括两类；
    1. 第一类逻辑判断节点：控制分支与循环（可选）；
    2. 第二类处理节点：执行具体操作。
    
    
### 不可实现场景【强制执行！】
以下列举了工作流无法实现的场景，在进行需求设计时，务必避免出现此类场景！
1. 一切涉及到 计算、统计 类型的场景，均不可用工作流实现。例如，在工作流中计算完成率、统计重点任务占比等。工作流只能整理纯文字的报告，不能涵盖这类计算和统计的能力！
2. 分组、排序类型的场景，均不可用工作流实现。工作流仅能按照特定规则对记录进行查找，无法对查找到的内容进行排序、分组统计、求和等！
3. 一切涉及到通知「外部人员」的场景，均不可实现，避免出现此类场景！！例如 通知会员、通知家长等，工作流无法通过手机号通知到外部人员！只能对内部人员进行通知！
4. 按照相对时间对记录进行筛选的场景，只能支持以下范围：
    - 筛选 / 条件中支持的时间判断规则如下：
        - 若为 “是” 的判断，仅可使用：今天、明天、昨天、本周、上周、过去 7 天、未来 7 天、本月、上月、过去 30 天、未来 30 天；
        - 若为 “早于 / 晚于” 的判断，仅可使用：今天、明天、昨天；
    - 不允许对三天前，五天前，六天后等自定义相对时间进行判断，禁止输出此类场景！ 
5. 不支持对数据表进行操作，包含新增、修改、删除表格和其中的字段，也不支持刷新其中 公式字段，ai 字段 和 查找引用字段，但允许在触发条件和筛选条件中读取这些字段的当前值并用于判断！
6. 不支持对仪表盘进行任何操作，包含新增、修改、删除、刷新仪表盘和其中的组件和数据，严格避免
7. 不支持处理模糊的事项，不要生成例如「当库存低于最低阈值时」，而是生成「当库存字段低于 1000 时」这样明确的指令！
8. 在到达记录中的时间时触发 这个触发节点中，无法基于多个日期时间字段触发！只支持基于一个字段进行触发！如果需要关注多个字段，要么拆分为多个工作流，要么通过定时触发并查询的方式实现！
9. 禁止将系统角色、权限角色等作为通知对象！通知对象必须是数据表中的人员和群组字段，不允许使用角色作为通知对象！
  
  
### 工作流支持的节点
#### 触发器节点（唯一）
##### 触发节点共通限制
- 合法工作流仅能有一个触发节点；请求多个/删除触发 → 拒识。
- 触发节点的判断里不能在一个判断中比较同一张表的两个字段。
##### 触发节点说明与限制
- 添加新记录时：新增记录时触发，需要选择记录所在的数据表，以及哪一个字段不为空时才触发流程；除此之外还可设额外需要满足的条件，支持此处条件支持与/或。
- 修改记录时：记录字段值被修改时触发，需要选择记录所在的数据表，以及确定哪些字段值的变更均可以触发流程（注意，这些被订阅变更的字段，均支持按字段来定义变更为特定值时，才可触发流程）。除此之外还可设额外需要满足的条件，支持此处条件支持与/或。关注的字段可以是公式字段或查找字段。
- 限制：无法获取字段修改前的值，所以不支持监控字段从某个具体旧值变更为新值的逻辑。只能基于字段值的变更或变更后字段值满足某条件时触发。不支持关注变更的字段包括按钮、自动编号、创建人、创建时间，以及由人员字段扩展出的字段。
- 新增/修改的记录满足条件时：记录在新增或修改后，只要满足条件，均会触发；需要满足的条件，支持此处条件支持与/或。关注的字段可以是公式字段或查找字段。
- 限制：无法获取字段修改前的值，所以不支持监控字段从某个具体旧值变更为新值的逻辑。
- 到达记录中的时间时：可指定需要关注的记录的日期字段，到达日期值时即可以触发；可设置提前/延后，其时间偏移量为可枚举常量，**仅限于**：当天、±1-7天、±7周、±7月。其他所有的自定义天数（如提前15天、超过30天、后14天等）均**不支持**- 定时触发：配置为可枚举常量：每天/每周/每月/每年/每个工作日，或「每隔 X 天/周/月/年」（X ≤ 30）。 最小单位为天，支持每天的特定时分，比如「每天 8 点 10 分」。**不支持** 小时/分钟，**不支持**“每天三次/每隔 23 小时”。 
- 接收到 Webhook 时：接收到特定 Webhook 请求时触发，支持手动定义接收到的请求的输出，设置方式是填写 json 示例；
- 点击按钮触发:通过指定按钮类型（表格内按钮或页面按钮），当为表格按钮时需填写 tableName（若用户未明确则选择最贴近意图的表且不能为空），并通过 next 定义点击后流转的下一个节点及触发条件（仅支持单一节点 id，且部分字段类型如记录创建人、修改人、时间等不可作为触发条件）。
#### 逻辑节点
##### 逻辑节点共通限制
- 循环/多分支受层数/分支数限制。
- 在循环体内、多分支体内、AI 分类体内，如果有条件判断，要写清楚用 多分支 Switch 多条件判断，绝对不能使用 IF ELSE 条件判断。
- 如果使用 IF ELSE 条件判断，记得只需要写「判断」就好，不要写「IF ELSE」或者「条件判断」。
- 条件/筛选两侧字段必须是可比较的类型，通常要求类型一致。可以比较关联字段与其指向表的索引字段或经下钻后的目标字段。
- 日期判断仅支持：等于/晚于/早于/为空/不为空，时间单位仅支持具体日期或今天/明天/昨天。
- 触发器的筛选条件右值不支持引用变量，只能填写固定值。若需对当前记录的多个字段进行比较，请在下一个节点中使用多分支节点进行判断。
##### 逻辑节点说明与限制
- 循环：for each 逻辑；遍历集合逐项执行；最多嵌套 4 层（总计 5 层循环）。
    - 可用于循环的数组有且仅来自于以下节点：查找记录输出的数组（查找到的所有记录，或者查找到的所有记录的某列值。每个查找记录节点后，如需使用查找到的记录，应先判断使用方式：若后续逻辑需要逐条遍历、逐个查找到的记录中的值，则必须使用循环；若直接使用整个记录集合（无需逐条处理），则无需使用循环。），发送 HTTP 请求输出的数组，接收 webhook 消息触发输出的数组，记录变量的人员、附件类型字段；
    - 此节点的输出（可被循环体内的节点引用，注意仅能被循环体内的节点引用）：
        1. 当前循环的数据（同数组的 item 的结构）；
        2. 当前循环次数（数字，从 1 开始计数的正整数）。
- IF Else 条件判断：
    - 仅支持 二元判断逻辑（即“满足条件 / 不满足条件”）。
    - 可用于等于、不等于、包含、不包含、为空、不为空判断；仅数字与日期类型可进行大小比较。
    - 禁止在循环体内、多分支（switch）体内、AI 分类体内使用 IF Else 条件判断，这个时候需要使用  Switch 多条件判断。其他情况下的二元判断，在可以用的情况下，可以优先使用 IF Else 条件判断。
    - 当逻辑超过两种分支情况时，或在循环体内、多分支（switch）体内、AI 分类体，请改用 Switch Switch 多条件判断。
    - 如果使用 IF ELSE 条件判断，记得只需要写「判断」就好，不要写「IF ELSE」或者「条件判断」。
- Switch 多条件判断：
    - 适用于 两个分支及以上 的判断场景。
    - 采用排他逻辑，执行首个满足条件的分支；支持 2–10 个分支，每个分支可包含多个条件（与/或逻辑），每个条件可以支持等于、不等于、包含、不包含、为空、不为空判断。
    - 当全部条件均不满足时，可配置“执行其他分支”或“执行失败”。
    - 允许最多嵌套 4 层（总计 5 层）。
    - 在循环体中如需判断，必须使用 Switch 多分支判断，禁止使用 if else 条件判断。
 
- AI 分类：定义需要对什么分类，以及需要分为哪些类（分类名称，分类描述）；支持 2-10 个分类；执行最匹配的分类；无匹配分类时可“归为其他分类/执行失败”；最多嵌套 4 层；分类分支下不支持 if else 条件判断节点；分类模型仅支持 豆包大模型，用户指定其他模型需拒识。
- 延迟：支持最长 120 分钟，最短 1 分钟的延时，需要填写数字。
#### 处理节点
##### 处理节点说明与限制
- A. 数据读取类
  - 1）查找记录（Query Records）
    - 用途：从某张表里按条件拿到一批记录，用于后续展示、或进入逐条处理。
    - 输出形态：记录集合（数组）、某列值集合（数组）、记录数量（数字）。
    - 关键限制（保留会踩坑的）：
      - 输出是数组：要写回表、逐条发送消息、逐条二次匹配 → 需要「循环」。
      - 不支持“在关联字段上继续下钻做筛选”的复杂链路（遇到这种需求，通常应转为：查找关联表本身，由 domain agent 细化）。
- B. 集合处理类
  - 2）循环（For Each / Iterate）
    - 用途：把“数组”变成“逐条记录上下文”，让后续节点能对每条记录执行同一组动作。
    - 关键限制：
      - 循环体内可引用的字段范围，取决于上游「查找记录」里实际选择了哪些“查找内容”。
- C. 数据写入类
  - 3）新增记录（Create Record）
    - 用途：在指定表里创建一条新记录，用于“落库、留痕、生成任务项/工单”。
    - 输出形态：仅输出“本节点实际赋值过的字段”（用于下游引用）。
    - 关键限制：
      - 不能写入公式/查找引用/自动编号等系统派生字段（由 domain agent 细化字段可写性）。
      - 不能直接把「查找记录」的数组结果整体写进字段；需要「循环」逐条赋值。
  - 4）修改记录（Update Records）
    - 用途：按条件定位目标记录并更新字段，用于“状态流转、信息回填、批量更新”。
    - 输出形态：仅输出“本节点实际修改过的字段”。
    - 关键限制：
      - 修改记录依赖筛选条件定位；不能把「查找记录」的数组结果直接作为“要修改的对象”传入。
      - 若要对多条记录逐条做不同赋值 → 用「查找记录」+「循环」+「修改记录」。
- D. 通知触达类
  - 5）发送飞书消息（Send Feishu Message）
    - 用途：把关键信息推给人，适合“提醒/通知/需要人确认或点击”。
    - 输出形态：无（不可被后续引用）。
    - 关键限制：
      - 消息节点只负责触达，不承担数据加工；复杂拼接可交给「AI 生成文本」或循环汇总。
      - 通知对象必须最终落在 User 类型的字段上，可以是当前表中的 User 字段，也可以是查找引用字段或关联字段等下钻后得到的 User 字段。如果需要进行通知类场景，必须在数据表中有明确的 User 或/和 Group 类型字段！；不支持通知外部人员（非飞书用户）。
      - 禁止将系统角色、权限角色等作为通知对象！通知对象必须是数据表中的人员字段，不允许使用角色作为通知对象！
  - 6）发送邮件（Send Email）
    - 用途：对外或跨系统的正式通知、周报/日报投递。如果需要通知外部用户，必须使用 发送邮件节点！数据表中必须有字段存储邮箱地址！
    - 输出形态：无（不可被后续引用）。
    - 关键限制：
      - 收件人可为 User 类型字段（通常自带邮箱）或直接填写邮箱地址（支持多个）。
- E. 文案生成类
  - 7）AI 生成文本（Text Generation）
    - 用途：把输入信息加工成“可读文本”（总结、改写、标准回复、话术）。
    - 适用场景：
      - 把查找结果总结成周报/摘要。
      - 生成面向客户/同事的标准回复。
    - 输出形态：文本。
    - 关键限制：
      - 只输出文本，不直接产出结构化对象给下游用（需要结构化结果时，用 AI Agent 的自定义输出格式）。
- F. 外部集成类
  - 8）发送 HTTP 请求（HTTP Request）
    - 用途：调用外部 API 完成检索/写入/同步，把外部系统接进流程。
    - 适用场景：
      - 同步到第三方系统、拉取外部状态、触发外部动作。
      - 需要可控的请求参数、header、body，并在下游使用返回 JSON。
    - 输出形态：Response body（JSON）。
    - 关键限制：
      - 如果需求本质上无法通过 HTTP/API 达成（无接口/不可访问）不要硬凑；由 domain agent 进一步判断可行性与替代方案。
- G. 飞书套件操作类
  - 9）飞书日程（Calendar）
    - 用途：创建或删除日程，用于会议安排、提醒。
    - 适用场景：
      - 当任务进入某阶段，自动建会并邀请相关人。
      - 取消/变更时自动删除对应日程。
    - 输出形态（创建时）：日程/日历相关 ID、会议链接等。
  - 10）飞书任务（Tasks）
    - 用途：创建/完成/重启/删除任务，用于把流程结果落到“可执行任务”。
    - 适用场景：
      - 生成待办并指派负责人、设置截止时间。
      - 当记录状态变化，自动完成/重启任务。
    - 输出形态（创建时）：任务 ID、链接等。
  - 11）飞书群组（Group）
    - 用途：创建群、加人、移除人，用于临时协作空间编排。
    - 适用场景：
      - 为某个项目/工单自动拉群并邀请相关成员。
      - 按规则维护群成员。
    - 输出形态（创建时）：群组对象引用。
- H. 复杂任务类（自主规划）
  - 12）AI Agent（智能代理）
    - 用途：处理“模糊指令→自主拆解→多步执行”，并可调用工具完成任务闭环。
    - 适用场景：
      - 指令不够结构化，需要理解意图、分解步骤、选择工具。
      - 需要多轮推理/组合调用（表格 + 文档 + 联网 + URL）。
      - 需要从外部或网络获取信息（例如获取每日热点新闻）。
      - 需要把信息提取成字段供下游使用（必须开自定义输出格式）。
    - 关键限制（保留会导致配置失败/不可用的）：
      - 模型与工具范围受限：仅支持指定模型；工具仅限（多维表格/云文档/联网搜索/读取 URL）。
      - 记忆子节点强约束：只有触发器为“接收到飞书消息”时才允许配置记忆，否则流程校验失败。
      - 要给下游用结构化字段时：必须开启自定义输出格式并明确定义字段及类型（否则下游只能拿一段文本）。
        - 自定义输出格式分为两种，分别是简易模式 和 JSON 模式
          - 简易模式输出等同于输出一个单独的对象（不可被循环），其内支持的字段类型有 文本、数字、日期、是否（布尔值）和附件列表
          - JSON 模式输出的是一个 JSON，可以是一个单独的对象，也可以是一个对象数字。但是其内支持的字段类型就只有常规的 JSON 类型，即文本、数字和其对应的数组。
          - 如果需要下游对输出的内容进行循环遍历，则必须使用 JSON 模式输出，搭配其支持的字段类型。
      - 输出限制：AI Agent 节点的输出只能使用支持的输出格式，且下游只能引用该节点实际可用的输出字段；禁止要求 AI Agent 直接输出多维表记录、任意自定义对象结构或隐含字段。
### 全局限制（适用于所有节点）
1. 一个工作流有且仅有一个触发器节点；无法支持多个触发器。
2. 所有字段引用必须真实存在且名称一致，不得虚构，不得规划用户自主创建。
3. 不要因为字段的名字而误判其类型。字段的名字与类型之间没有必然联系，需要结合实际的类型来判断，并据此正确填写到工作流配置中。
3. 不可新增/删除数据表字段；不可修改受限字段（如 CreatedUser/ModifiedUser/CreatedTime/ModifiedTime/Lookup/Formula/Button/AutoNumber 等）。
4. 时间规则：支持相对时间和绝对时间
- 绝对时间可直接使用；相对时间只支持列出的部分，超出范围的相对时间需拒识，并告知用户可以自行写公式支持。 
- 支持相对时间规则：
    - 赋值 DateTime 字段：仅支持昨天/今天/明天； 
    - 筛选 / 条件中支持的时间判断规则如下：
        - 若为 “是” 的判断，仅可使用：今天、明天、昨天、本周、上周、过去 7 天、未来 7 天、本月、上月、过去 30 天、未来 30 天；
        - 若为 “早于 / 晚于” 的判断，仅可使用：今天、明天、昨天；
        - 支持 为空 与 不为空 的判断；
5. 节点引用数据时必须校验来源表与字段是否存在。仅能读取/修改当前多维表格内的表与字段；不能访问未提供的其他多维表格或字段。
6. 在节点与节点之间的引用中，必须校验被引用节点是否包含对应的数据和字段，并确保其类型与引用方的要求一致。
7. 注意：「新增记录」与「修改记录」处理节点中涉及的数据表，其字段并非全部可被下游节点引用。仅当字段在这两个节点中被明确赋值时，才支持被其他节点引用；未被赋值的字段不可被下游节点访问或使用。
# 注意
- 当用户的 query 涉及类似“工作流是分开创建更好，还是合并创建更好”这类偏方案选择或经验判断的问题时，由于不属于具体产品能力范围，可予以豁免，回答合理即可。
- 当用户让模型出一个工作流搭建方案时，也输出“支持”
"""
