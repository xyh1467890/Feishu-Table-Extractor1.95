def get_advanced_permission_prompt(query: str) -> str:
    return f"""

# 用户输入
请判断以下 Query 是否在高级权限产品的真实能力范围内：
Query: {query}

请判断：
- 当前 Query 是否在该产品真实能力范围内
- 是否超出产品能力边界
- 是否存在不合理或无法实现的诉求

# 限制要求
请严格按照以下 JSON 格式输出，不要包含任何其他文字：
{{
    "within_ability_scope": true/false,
    "reason": "能力边界判断说明"
}}

## 高级权限产品介绍
本能力用于描述 AI 在多维表格高级权限场景下可理解的权限模型与可执行的配置边界，作为生成权限设计方案的能力约束依据

### 什么是「角色」

在多维表格高级权限中，**角色** 是一组权限配置的集合，用于定义一类成员在多维表格中的可操作范围。

- 一个角色可以被分配给多个成员  
- 一个成员可以同时被分配多个角色  
- 成员的最终权限由其所属的所有角色共同生效  

角色分为两类：

- **系统角色**：所有者、管理员、编辑者、阅读者  
- **自定义角色**：由用户创建并配置的角色  

### 权限对象与层级结构

高级权限支持对以下对象进行权限控制：

- 数据表 
- 记录（行）  
- 字段（列）  
- 视图  
- 仪表盘  
- 其他功能权限（复制、下载等）  

其中，权限存在明确的层级关系：

**数据表 ＞ 记录 ＞ 字段**

- 数据表权限决定该角色在该表上的能力上限  
- 记录与字段权限不能突破表权限允许的范围  
- 视图权限以表权限为上限，但不参与字段能力封顶  

### 数据表权限

数据表权限定义角色在整张表上的总体能力边界，共有四种：可管理、可编辑、仅可阅读、无权限。

表权限是所有下层权限的前置条件与上限：

- 表权限为 `无权限`：不能再配置任何记录、字段、视图权限  
- 表权限为 `仅可阅读`：所有新增 / 编辑 / 删除能力均不可用  
- 表权限为 `可编辑`：允许配置记录与字段的新增、编辑、删除等权限，但不允许管理表结构与权限  
- 表权限为 `可管理`：允许管理该表的所有权限配置与结构设置
- 一旦设置为可管理，该表下禁止再配置任何记录 / 字段 / 视图的细项权限（终态权限）


### 记录权限

记录权限用于控制可以对哪些记录进行哪些操作。

#### 记录操作权限

- 可新增记录  
- 可删除记录  

是否允许新增 / 删除记录，与字段是否可新增 / 可编辑存在依赖关系。

#### 记录范围（满足特定条件的记录）

当角色不能操作所有记录时，可以通过 **筛选条件** 限定范围，例如：

- 只能查看 / 编辑满足某些条件的记录  
- 条件由 **字段 + 算子 + 值** 组成，**值**是具体的值或选项 
- 支持 **AND / OR 的条件组**（条件组是系统能力的一部分）  


### 字段权限

字段权限用于控制角色在字段（列）维度上的能力。

#### 字段权限模式

字段权限有四种配置模式：

- 所有字段内容可编辑
- 所有字段可阅读 
- 自定义权限
- 无权限

只有在 **自定义权限** 模式下，才可以对单个字段做精细配置。

#### 字段内容权限

对单个字段可设置：

- 可阅读
- 可新增 
- 可编辑
- 无权限  

字段权限存在固定的自动继承关系：

- 可编辑 ⇒ 自动获得 **可新增 + 可阅读**  
- 可新增 ⇒ 自动获得 **可阅读**  

### 系统字段与自动字段

#### 系统字段（完全不可配置）

系统字段指由系统自动维护、用于记录数据元信息的字段，包括：

- 创建人（CreatedUser）  
- 修改人（ModifiedUser） 
- 创建时间（CreatedTime）
- 修改时间（ModifiedTime）

系统字段具有以下特性：

- 字段权限 **固定为可阅读**  
- 字段内容由系统自动维护
- 不允许配置可新增或可编辑  

系统字段的上述约束为系统级规则，优先级高于任何用户配置或 Agent 输出。


#### 自动字段（内容不可编辑，但可配置可见性）

自动字段指字段值由系统自动填入或计算生成的字段，包括：

- 公式字段（Formula）  
- 查找引用字段（Lookup）  
- 自动编号字段（AutoNumber）  
- 人员字段「获取更多信息」生成的拓展字段  

自动字段具有以下特性：

- 字段内容不支持人工新增或编辑  
- 字段值随来源变化自动更新  
- 字段权限 **仅允许可配置为可阅读或无权限**  
- 不支持设置为可新增或可编辑  

自动字段的内容行为由系统约束，但其**可见性仍受字段权限控制**。


### 单选 / 多选字段的选项权限

对于单选、多选字段，除字段内容权限外，还存在一类 **选项级能力**：

- 可增删改选项  

该能力仅在满足以下条件时才可能生效：

- 数据表权限为 **可编辑或可管理**  
- 字段本身为 **可编辑**  
- 字段权限模式为 **自定义权限**  


### 附件字段与导出能力

附件字段除字段内容权限外，还涉及 **附件导出（下载）能力**。

是否允许导出附件，需同时满足以下条件：

1. 表权限 ≥ 可阅读  
2. 字段权限允许阅读  
3. 高级权限中允许「创建副本、下载、打印多维表格」  
4. 通用权限中允许下载（由系统统一控制）  

任一条件不满足，附件导出能力即不可用。


### 筛选条件的基础概念

筛选条件用于限定「满足特定条件的记录」。

结构约束（必须遵守）
- 筛选字段必须来自当前数据表中真实存在的字段，不允许跨表引用
- 筛选条件必须是基于单一字段的直接判断；不得穿透关联表（SingleLink / DuplexLink），不得用“关联X的字段Y满足…”来表达筛选条件
- 不允许基于其他数据表字段或多级关系推导“本人/自己的数据”（例如“关联收购记录的农户=当前用户”属于非法表达）
- 右值约束：筛选条件描述分为左值和右值，如“所属区域为华北”中，“所属区域”为左值，“华北”为右值，右值只能是具体的值，而不能是二级推导，如“所属区域为华北”的描述是正确的，但如“所属区域为当前用户负责区域”、“所属区域为当前区域”的描述是错误的，因为“当前用户负责区域”、“当前区域”为二级推导；
- 当以 user 字段作为筛选条件时，右值只能是角色自己原值，不能是角色自己的扩展信息（比如本人的 xxx）：举例如“部门负责人是当前用户/本人”的描述是正确的，但“所属部门为当前用户/本人的部门”的描述是错误的；


字段类型约束（系统硬限制，必须遵守）
- 只能将以下类型字段设置为筛选条件，非下述所列字段一定不能作为筛选条件：
    - 单选（SingleSelect）、多选（MultiSelect）
    - 人员字段：人员（User）、创建人（CreatedUser）
    - 流程字段（Stage）
    - 数字类：Number / Progress / Rating / Currency
    - 公式字段（Formula）与查找引用字段（Lookup）：仅当 table 文档中明确说明其“输出结果类型属于上述可筛选类型，且取值为原值（非聚合/非拼接/非跨表推导）”时才允许作为筛选字段
- 不支持的字段，即使语义合理，也不能作为筛选条件 

### “本人 / 自己数据”逻辑识别
当需求涉及“只能查看 / 操作自己数据”时：

- Agent 仅允许使用**当前数据表中真实存在的人员字段（User / CreatedUser）**来表达该语义
- 不允许通过关联表、查找引用字段或多级关系推导“本人数据”
- 若当前数据表不存在可用于表达“本人”的字段，则不得生成该表的“本人数据”记录筛选方案
- 本人隔离只允许用当前表中的 User/CreatedUser 直判；找不到就不得生成本人隔离规则
- 当以 user 字段作为筛选条件时，右值只能是角色自己原值，不能是角色自己的扩展信息（比如本人的 xxx）

### 视图权限
配置项包括
- 是否可「新增、删除、修改视图」
- 可见的视图：可选「所有视图」或「指定视图」
 
配置原则：
- 当数据表的权限设置为 可编辑 时，才可以设置角色是否 可新增、删除、修改视图。
- 若 可见的视图 选择的是 指定视图，则无法再勾选 可新增、删除、修改视图。


### 仪表盘权限
- 仪表盘阅读权限（仅可阅读 / 无权限）

### 其他权限
- 是否可“复制多维表格内容”
- 是否可“创建副本、下载、打印多维表格”

### 系统角色的能力边界

系统角色的能力由系统预设，存在不可变约束：

- **所有者 / 管理员**
  - 权限不可被修改  
  - 不可被降级或重写  

- **编辑者/ 阅读者**
  - 权限可被修改  
  - 编辑者 可修改范围仅限于数据表、记录、字段、视图等高级权限项，不包含仪表盘编辑、工作流或系统级配置能力
  - 阅读者 永远不允许拥有新增、编辑、删除能力  

## AI 目前支持的高级权限**能力范围**（一定不能超出）

### 可操作范围

仅支持：
1) **创建角色并配置权限**：仅限自定义角色；  
2) **修改角色并配置权限**
    - 修改系统角色配置（仅限“编辑者”、“阅读者”，不包含“所有者”、“管理员”角色），且仅能修改具体的权限配置，不能修改角色名称或删除角色；
    - 修改已有自定义角色配置，且仅能修改具体的权限配置，不能修改角色名称或删除角色； 

### 可配置项

#### 数据表整体权限
- 可管理 / 可编辑 / 仅可阅读 / 无权限

#### 记录权限
- 可对记录进行的操作（如：新增/删除等）
- 可编辑（和删除）的记录范围
- 可阅读的记录范围

#### 字段权限
- 字段内容权限（可阅读 / 可新增 / 可编辑 / 无权限）
- 单选、多选字段的选项权限
- 附件字段的下载权限
- 字段整体权限

#### 视图权限
- 是否可“新增、删除、修改视图”
- 可见的视图

#### 仪表盘权限
- 仪表盘阅读权限（仅可阅读 / 无权限）

#### 其他权限
- 是否可“复制多维表格内容”
- 是否可“创建副本、下载、打印多维表格”

"""
