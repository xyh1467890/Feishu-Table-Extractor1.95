def get_dashboard_prompt(query: str) -> str:
    return f"""
# 用户输入
请判断以下 Query 是否在仪表盘产品的真实能力范围内：
Query: {query}

请判断：
- 当前 Query 是否在该产品真实能力范围内
- 是否超出产品能力边界
- 是否存在不合理或无法实现的诉求

# 限制
请严格按照以下 JSON 格式输出，不要包含任何其他文字：
{{
    "within_ability_scope": true/false,
    "reason": "能力边界判断说明"
}}

## 多维表格仪表盘产品介绍

仪表盘是多维表格中的数据可视化模块，通过图表、指标卡、表格等组件将数据表中的数据进行可视化展现，帮助用户快速获取业务洞察、监控关键指标并支持数据驱动决策。

本文档用于说明仪表盘在“基于既有数据表结构”前提下可实现的展示能力、组件能力、配置边界与不支持项。输入信息通常来自数据表设计文档（包含数据表名称、字段名称、字段类型、字段描述等）。

### 仪表盘构成要素

- 仪表盘名称：标识仪表盘的唯一名称，应与业务场景相关。
- 主题样式：定义仪表盘的整体视觉风格。
- 组件集合：由多个可视化组件组成，每个组件独立配置数据源与展示方式。
- 切片器联动：通过切片器组件实现全局数据筛选与联动控制。

### 主题样式

选择主题时，优先考虑可读性、专业感、长时间观看舒适度、信息密度适配度。若需要值守、低炫光，优先选择深色类主题。

| 主题标识     | 名称                  | 适用场景                                   |
| ------------ | --------------------- | ------------------------------------------ |
| default      | 默认                  | 灰底+蓝黑白配色，适合标准需求              |
| SimpleBlue   | 明晰 / Marine blue    | 纯白底+亮蓝配色，适合高可读、专业、通用    |
| DarkGreen    | 夜览 / Midnight green | 深黑底+青绿配色，适合夜间、大屏、持续观看  |
| summerBreeze | 清新 / Fresh          | 淡蓝底+浅蓝配色                            |
| simplistic   | 简约 / Minimalist     | 纯白底+灰橙配色，适合信息密度高、稳重、CRM |
| energetic    | 活力 / Vibrant        | 米白底+橙配色                              |
| deepDark     | 深邃 / Dark           | 深蓝底+绿配色，适合企业数据、交易类场景    |
| futuristic   | 未来 / Futuristic     | 深紫底+亮蓝配色，适合科技感、创新业务      |

### 仪表盘组件

#### 组件通用配置

| 配置项   | 说明                                                      |
| -------- | --------------------------------------------------------- |
| 分组维度 | 类别或分组字段，如“地区”“渠道”“品类”，用于分组依据        |
| 统计指标 | 记录总数或数值字段的聚合，支持 `SUM`、`AVG`、`MAX`、`MIN` |
| 排序方式 | 按字段值或记录顺序正序或倒序排列（含 TopN 的意图描述）    |
| 筛选条件 | 数据范围筛选条件，支持多条件与或组合                      |

#### 图表口径与轴字段约束

- 类别轴字段禁止聚合计算：禁止对类别轴字段做任何聚合计算（禁止对日期字段聚合为“按月”或“按周”）。
- 数量统计：按记录数 COUNT。
- 数值统计：按字段数值做聚合（`SUM`、`AVG`、`MAX`、`MIN`）。
- 若需求同时出现“数量统计”和“数值统计”，应拆分为多个图表组件分别呈现。
- 单图表多指标共享配置：当一个组件使用多个数值指标时，所有指标共享同一套筛选条件和聚合方式（`SUM`、`AVG`、`MAX`、`MIN` 四选一），不支持为每个指标单独设置不同的筛选条件或聚合方式；若需对同一字段按不同筛选口径分别统计（如对「金额」字段分别统计类型为"收入"和"支出"），必须拆分为多个独立组件或使用多数据源方案。

### 组件类型列表

#### 指标类组件

| 组件名称 | chart_type | 说明                                                         |
| -------- | ---------- | ------------------------------------------------------------ |
| 指标卡   | statistics | 选择 1 张数据表的 1 个字段做聚合（或记录总数）；可开启同环比、趋势图（需时间字段） |
| 进度图   | progress   | 支持数值进度与日期进度两种口径；数值进度配置当前值与目标值（字段或固定值）；日期进度支持本月、本季度、本年或自定义起止，当前日期可选昨日、今日或自定义日期；支持条形、半环形或圆环形 |
| 倒计时   | countdown  | 目标时间来自固定值或日期字段；当目标来自日期字段时可按最早或最晚日期计时；展示粒度支持月、周、天、时、分、秒 |

#### 趋势类组件

支持配置：1 个时间/有序字段 + 1 到 3 个指标，或者 1 个类别字段 + 1 到 3 个指标。

| 组件名称         | chart_type                     |
| ---------------- | ------------------------------ |
| 折线图           | line                           |
| 平滑折线图       | smooth_line                    |
| 阶梯折线图       | ladder_line                    |
| 基础柱状图       | histogram                      |
| 堆积柱状图       | aggregate_histogram            |
| 百分比堆积柱状图 | percentage_aggregate_histogram |
| 基础条形图       | bar                            |
| 堆积条形图       | aggregate_bar                  |
| 百分比堆积条形图 | percentage_aggregate_bar       |

#### 面积图组件

支持配置：1 个类别字段 + 1 到 3 个指标。

| 组件名称                  | chart_type             |
| ------------------------- | ---------------------- |
| 面积图（不堆积·折线）     | area_nostack_line      |
| 面积图（不堆积·平滑）     | area_nostack_smooth    |
| 面积图（不堆积·阶梯）     | area_nostack_ladder    |
| 面积图（堆积·折线）       | area_stack_line        |
| 面积图（堆积·平滑）       | area_stack_smooth      |
| 面积图（堆积·阶梯）       | area_stack_ladder      |
| 面积图（百分比堆积·折线） | area_percentage_line   |
| 面积图（百分比堆积·平滑） | area_percentage_smooth |
| 面积图（百分比堆积·阶梯） | area_percentage_ladder |

#### 组合图组件

支持配置：1 个类别字段 + 1 到 3 个指标。仅当用户明确要求“组合图”“柱线图”“双轴图”等表达时使用：

| 组件名称                         | chart_type              |
| -------------------------------- | ----------------------- |
| 组合图（不堆积·柱+折线）         | combo_nostack_line      |
| 组合图（不堆积·柱+平滑折线）     | combo_nostack_smooth    |
| 组合图（不堆积·柱+阶梯）         | combo_nostack_ladder    |
| 组合图（堆积·柱+折线）           | combo_stack_line        |
| 组合图（堆积·柱+平滑折线）       | combo_stack_smooth      |
| 组合图（堆积·柱+阶梯）           | combo_stack_ladder      |
| 组合图（百分比堆积·柱+折线）     | combo_percentage_line   |
| 组合图（百分比堆积·柱+平滑折线） | combo_percentage_smooth |
| 组合图（百分比堆积·柱+阶梯）     | combo_percentage_ladder |

#### 结构或占比类组件

| 组件名称   | chart_type | 说明                                                         |
| ---------- | ---------- | ------------------------------------------------------------ |
| 饼图       | pie        | 1 个类别字段 + 1 个指标                                      |
| 环形图     | ring       | 配置同饼图                                                   |
| 漏斗图     | funnel     | 按阶段字段顺序配置各层 + 指标；仅当数据表中存在可直接表示阶段顺序的字段时使用 |
| 雷达图     | radar      | 1 个类别字段 + 多个指标                                      |
| 填充雷达图 | area_radar | 配置同雷达图                                                 |
| 散点图     | scatter    | 横轴、纵轴各选 1 个指标                                      |
| 词云       | word_cloud | 选择关键词字段与统计方式                                     |

#### 排行或明细类组件

| 组件名称 | chart_type  | 说明                                                      |
| -------- | ----------- | --------------------------------------------------------- |
| 排行榜   | ranking     | 排名对象（类别字段）+ 排行依据（指标）+ TopN 数           |
| 表格     | grid_view   | 只选择 1 张数据表，支持筛选、排序，禁止选择视图或显隐字段 |
| 透视表   | pivot_table | 配置行维度、列维度、指标字段，禁止配置聚合或指标计算      |

#### 辅助类组件

| 组件名称 | chart_type | 说明                                              |
| -------- | ---------- | ------------------------------------------------- |
| 文本     | text       | 描述具体文本内容                                  |
| 切片器   | slicer     | 选择 1 个筛选字段；样式可选平铺、下拉、数值、日期 |
| NPS 组件 | nps        | 选择 1 个评分字段，配置分值区间划分               |

### 多数据源组件配置能力与约束

当一个组件需要使用多张数据表时，遵循以下规则：

- 禁止以下组件使用多数据源表：`statistics`、`nps`、`grid_view`、`pivot_table` ；如需多表展示，应拆成多个独立组件。
- 关联限制：仅允许 UNION ALL（纵向拼接），禁止 JOIN（横向关联），禁止通过字段做跨表关联计算。
- 分组字段类型一致：各数据表用于分组的字段类型必须完全一致（例如都为日期类型或都为文本类型）。
- 每个数据源最多 1 个数值指标字段：多数据源场景中，每张表最多选择 1 个数值字段作为指标，若同一张表需要多个指标或多个筛选口径，应拆分为多个独立组件。
- 数据源去重：同一组件内，同一张数据表只能出现一次，禁止以不同筛选条件重复添加同一张表。
- 记录数场景：全部使用 COUNT。
- 数值统计场景：所有数据表的指标必须保持使用同一种聚合方式（`SUM`、`AVG`、`MAX`、`MIN` 之一），不得混用 COUNT 与数值聚合。

### 字段引用与关联字段约束

- 组件中的分组/横轴/纵轴/聚合/筛选/排序/排行/联动字段，必须来自当前组件选定的数据源表。
- 不支持直接引用关联表中的原始字段作为组件配置字段；如需使用关联信息，该字段必须已经在当前数据源表中以 Lookup / Formula 字段形式存在。

### 指标卡配置要点与约束

- 单字段聚合：指标卡只支持对 1 个字段做聚合，或统计记录总数。
- 字段类型要求：指标卡若不是统计记录总数，而是对字段做聚合，则该字段应为可产出数值的字段（如数字字段、数值型公式字段、引用数值字段的查找引用字段）。
- 同环比与趋势图依赖：开启同环比或趋势图时，数据表必须存在可用时间字段。
- 默认不开启同环比和趋势图：除非 Requirement 明确要求，否则不要在描述中主动扩展这两类配置。
- 百分比或完成率类字段：禁止对比例类字段做求和；优先使用平均值。
- 禁止多字段计算：不支持字段间加减乘除、差值、A 除以 B、以及“占比类指标”的推导计算。

### 切片器配置要点与约束

- 位置：建议放置于首行，或指标卡、趋势图组件的上方。
- 组件关联：当切片器使用某字段时，全局所有组件均禁止该字段作为筛选项。
- 默认值：切片器禁止描述默认值。

### 比率/率类指标处理规则

当需求中出现"通过率""出勤率""转化率""完成率"等比率类指标时，按以下优先级处理：

1. **数据表设计文档中已有对应计算好的比率字段**（如字段名包含"出勤率""通过率"等）→ 直接在指标卡使用该字段做 AVG（禁止 SUM），这是最优方案
2. **数据表中没有比率字段，但同一张表内可以把分子/分母分别表达为“筛选后的记录数”或“筛选后的单字段聚合”** → 可使用 `progress` 组件表达当前值/目标值；这条规则特别适用于 Requirement 明确点名的“完成率”“转化率”
3. **如果 Requirement 没有明确点名“XX率”，且只能拿到分子字段**（如"通过次数""签到人数"）→ 使用分子字段做 SUM 或 COUNT，并在组件名称中体现实际统计口径（如改为"签到人数统计"而非"出勤率"）
4. **除 `progress` 的当前值/目标值方案外，禁止自行推导比率**：不得在其他组件描述中写"A/B""A占B比例"等多字段计算表达式
5. **若某个显式率类/时长类需求既没有现成字段，也无法用 `progress` 的当前值/目标值方案落地**，不要用 `text` 解释组件冒充覆盖；该需求视为当前数据表下不可直接执行，只能继续保证其他组件全部可执行

### 明确不支持的能力

- 禁止创建或修改：看板视图、日历视图、甘特视图、画册视图、组合布局组件。
- 禁止修改：背景色或背景图、扩展插件、图表联动配置、TopN 内部算法配置、千人千面能力。
- 禁止类型互转：指标卡、表格、透视表、文本、排行榜、进度图、倒计时、NPS 图、切片器。
- 组合图限制：`combo_*` 不支持指定左右轴位置及类型。
- 指标计算限制：不支持去重计数、不支持占比计算、不支持任何形式的多字段计算。
- 指标级筛选：不支持在同一组件内为不同指标配置独立的筛选条件（如同一张表的同一字段按不同条件分别聚合）。
"""
