def get_multidimensional_table_prompt(query: str) -> str:
    return f"""
# 用户输入
请判断以下 Query 是否在飞书多维表格产品的真实能力范围内：
Query: {query}

请判断：
- 当前 Query 是否在该产品真实能力范围内
- 是否超出产品能力边界
- 是否存在不合理或无法实现的诉求

# 限制
请严格按照以下 JSON 格式输出，不要包含任何其他文字：
{{
    "within_ability_scope": true/false,
    "reason": "能力边界判断说明"
}}

## 多维表格数据表产品介绍
数据表是多维表格中承载数据的基本容器，用于组织某一类业务实体的数据集合。数据表由字段组成结构，由记录构成数据，其展示方式由视图管理。
- 字段是定义数据表结构的列，用于描述记录在某一属性上的数据形态。字段属于结构层，不直接承载具体数据，并决定记录在对应属性上的类型与格式。
- 记录是数据表中结构化数据的实例，由字段在数据层的实际取值共同构成。记录属于数据层，是业务对象在表中的具体表示，不包含结构信息。
- 视图基于展示规则呈现数据，用于对记录进行筛选、排序与布局展示。视图不存储数据，只通过规则控制呈现方式。
### 数据表能力与限制
- 多表结构：一个多维表格系统可以包含多张数据表；每张数据表由字段和记录组成，默认包含一个以表格视图呈现的主视图，也可根据需要创建多个视图。  
- 命名规范（约束）：数据表名称长度需在 1–100 个字符之间；数据表名 **禁止/不能** 包含 `/ \ ? * [ ]` 等特殊字符，若出现上述字符，**必须**移除掉这些特殊字符，再进行后续继续处理。 
- 索引列：也称主键，每个数据表必须且仅有一个索引列，用于唯一标识记录。该列的取值不可重复，且不可删除或隐藏。 
- 可设为索引列的字段类型（仅限以下）：文本、日期、数字、超链接、公式、自动编号、电话号码、Email、地理位置、条码、进度、货币。除上述类型外，均不可设置为索引列。 
### 字段能力与限制
- 多维表格支持 **基础字段类型**、**4 种扩展字段类型**（公式、单向关联、双向关联、查找引用）与 **6 种 AI 能力字段类型**。  其中，能够引用或使用非当前表数据的字段包括：单选：（SingleSelect）、公式（Formula）、查找引用（Lookup）、单向关联（SingleLink）、双向关联（DuplexLink）。
- 所有字段均不支持默认值
- 字段名称约束：字段名称长度需在 1–1000 个字符之间；数据表名 **禁止/不能** 包含 "[" 和 "]" 特殊字符，若出现上述字符，**必须**移除掉这些特殊字符，再进行后续继续处理。
- **字段类型强制约束**：姓名、xx人字段必须为人员 User 类型（如：姓名、负责人、审批人、经办人等）。**注意**：创建人（CreatedUser）、修改人（ModifiedUser）是系统字段，不受此规则约束，它们本身就是 User 类型。 
- **字段类型强制约束**：字段类型必须属于多维表格现有支持字段类型列表，严格依据已定义的字段能力来满足用户需求。  当用户描述的字段类型不在系统支持的字段类型列表中时，应结合用户的意图、现有数据表结构和字段配置，综合判断并调整为最合适的已支持字段类型。 已支持的字段类型里列表、约束与限制如所示：
#### 基础字段类型    
- 文本（Text）：存储字符串
- 单选（SingleSelect）：选择一个选项
- 多选（MultiSelect）：选择一个或多个选项
- 人员（User）：选择一个或多个飞书成员，仅支持选择飞书可检索成员
- 日期（DateTime）：存储日期或日期+时间，日期格式仅支持 yyyy-MM-dd、yyyy-MM-dd HH:mm，不支持设置时间段
- 附件（Attachment）：上传文件，可设置"仅可通过移动端拍照录入"；附件类型可设置"仅图片和视频"
- 数字（Number）：存储数值，数字格式仅支持 '整数'、'千分位'、'千分位（小数点）'、'保留 n 位小数（n=1–9）'、'百分比'、'百分比（小数点）' 格式
    - 千分位和百分比带小数格式默认为两位小数，且不支持设置小数位数，如要求设置小数位数直接拒识
    - 百分比类型的值会自动换算，无需手动乘 100，如百分比类型输入为 1，则自动换算为 100%
- 复选框（Checkbox）：布尔状态（是/否）
- 流程（Stage)：多节点顺序流程；单元格记录当前节点状态
- 自动编号（AutoNumber）：为记录生成唯一递增编号，不可手动编辑字段值，支持自增数字和自定义编号两种模式
    - 自增数字为从1开始的自然数递增，支持设置 1-9 位的长度；
    - 自定义编号支持多种编号规则组合，多个规则通过字符串拼接
        - 自增数字（必选，支持设置 1-9 位的长度）
        - 固定字符
        - 创建日期（yyyyMMdd、yyyyMM、yyMM、MMdd、MM、dd 7 种格式） 
- 超链接（Url）：存储可跳转 URL
- 电话号码（Phone）：需通过号码格式校验
- Email（Email）：需通过邮箱格式校验
- 地理位置（Location）：可输入任意位置（默认）
- 条码（Barcode）：默认任意方式录入；
- 进度（Progress）：数值（0–100）或百分比（0%–100%）
    - 精度：仅支持整数、保留 1 位小数、保留 2 位小数
    - 不支持：精度超过 2 位小数，数值超过 100
- 货币（Currency）：货币类型 + 数值
    - 货币类型仅支持：人民币、美元、欧元、英镑、阿联酋迪拉姆、澳大利亚元、巴西雷亚尔、加拿大元、瑞士法郎、港元、印度卢比、印尼盾、日元、韩元、澳门元、墨西哥比索、马来西亚令吉、菲律宾比索、波兰兹罗提、俄罗斯卢布、新加坡元、泰国铢、土耳其里拉、新台币、越南盾
    - 小数位：整数、保留 1/2/3/4 位小数
- 评分（Rating）：评分符号 + 分值
    - 分值范围：起点 0 或 1；终点 1–10 整数
    - 符号只支持：星星、爱心、赞、火、笑脸、闪电、花、数字
#### 系统字段类型
- 创建人（CreatedUser）：系统字段，不可修改值
- 修改人（ModifiedUser）：系统字段，不可修改值
- 创建时间（CreatedTime）：系统字段，不可修改值
- 最后更新时间（ModifiedTime）：系统字段，不可修改值，支持设置修改来源，可选"任一字段修改时更新"或"指定字段修改时更新"（可选择一个或多个字段作为来源）
#### 扩展字段类型
- 公式（Formula）：可引用当前表或其他表字段进行计算；支持跨表引用 `表名.字段名`；
    - 公式计算结果需要指定一个输出类型，仅支持：文本/日期/数字，不支持单选、复选框格式
    - 计算结果的格式必须符合对应类型的格式要求
    - 若公式字段要求输出"是"或"否"，则只能输出为"是"和"否"的文本
- 单向关联（SingleLink）：关联一个目标表记录，支持单个记录和多个记录，关联字段的值为关联记录的索引列
    - 仅能选 1 个目标表
    - 支持指定关联的纪录范围，指定后关联字段只能在指定的范围内关联记录，支持选择"全部记录"或"指定记录"
    - 指定记录关联范围需给出筛选条件（最多 5 条，左侧必须目标表字段，右值必须为常量值，筛选的判断规则随字段类型变化）
        - 若目标表字段为文本、超链接、电话号码、Email、地理位置、条码、总结、翻译、信息提取、单选、多选、分类、智能标签、流程、人员、附件、单向关联、双向关联、创建人、修改人字段，支持「为空 / 不为空 / 等于 / 不等于 / 包含」判断；
        - 若目标表字段类型为数字、自动编号、进度、货币、评分，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 大于 / 大于或等于 / 小于或等于 / 小于」判断；
        - 若目标表字段类型为日期、创建时间、最后更新时间，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 晚于 / 大于或等于 / 小于或等于 / 早于」判断；
    - 其他字段/视图引用该关联字段时：比较与展示仅使用目标表索引列值
- 双向关联（DuplexLink）：关联一个目标表记录，支持单个记录和多个记录，并在目标表自动生成反向关联的字段，关联字段的值为关联记录的索引列
    - 仅能选 1 个目标表
    - 指向其他表会在目标表自动生成反向字段（字段名固定为源表原始表名，禁止改名/别称）
    - 支持指定关联的纪录范围，指定后关联字段只能在指定的范围内关联记录，支持选择"全部记录"或"指定记录"
    - 指定记录关联范围需给出筛选条件（最多 5 条，左侧必须目标表字段，右值必须为常量值，筛选的判断规则随字段类型变化）
        - 若目标表字段为文本、超链接、电话号码、Email、地理位置、条码、总结、翻译、信息提取、单选、多选、分类、智能标签、流程、人员、附件、单向关联、双向关联、创建人、修改人字段，支持「为空 / 不为空 / 等于 / 不等于 / 包含」判断；
        - 若目标表字段类型为数字、自动编号、进度、货币、评分，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 大于 / 大于或等于 / 小于或等于 / 小于」判断；
        - 若目标表字段类型为日期、创建时间、最后更新时间，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 晚于 / 大于或等于 / 小于或等于 / 早于」判断；
    - 其他字段/视图引用该关联字段时：比较与展示仅使用目标表索引列值
- 查找引用（Lookup）：根据指定查找条件从引用表的某个字段取值，根据引用字段的原值进行原值/去重/去重计数/计数/求和/平均/最大/最小的计算，必须设置：引用表+引用字段+查找条件（≥1，≤5）+计算方式+字段格式
    - 引用表：必须选择已有的数据表，禁止选择当前表
    - 引用字段：必须选择引用表的已有字段，只支持引用一个字段，禁止引用多个字段；
    - 查找条件：至少 1 条，最多 5 条，多个查找条件必须设定「且」（即「符合以下所有条件」）与「或」（即「符合以下任一条件」），默认选择且，每个查找条件必须设定左值（必须是引用表字段）、判断逻辑、右值（必须是当前表字段或常量值，禁止引用其他表字段，禁止对当前表字段值做二次计算）
        - 若引用表字段为文本、超链接、电话号码、Email、地理位置、条码、总结、翻译、信息提取、单选、多选、分类、智能标签、流程、人员、附件、单向关联、双向关联、创建人、修改人字段，支持「为空 / 不为空 / 等于 / 不等于 / 包含」判断；
        - 若引用表字段类型为数字、自动编号、进度、货币、评分，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 大于 / 大于或等于 / 小于或等于 / 小于」判断；
        - 若引用表字段类型为日期、创建时间、最后更新时间，支持「为空 / 不为空 / 等于 / 不等于 / 包含 / 晚于 / 大于或等于 / 小于或等于 / 早于」判断；右侧常量支持 昨天、今天、明天和某个具体日期（yyyy/MM/dd），不支持 7 天、30 天、本月、本周 等动态日期
        - 所有查找条件只能发生在引用表和当前表之间，禁止将其他不相关的表和字段加入到查找条件
    - 计算方式：原值/去重/去重计数/计数/求和/平均/最大/最小，不支持其他计算，不支持在引用的多个字段值中取最新、最晚这类计算
    - 字段格式：仅支持默认/文本/数字/日期/货币/自动编号，其余展示格式不支持
        - 原值/去重：可选"默认"或 等于引用字段的格式
        - 去重计数/计数/求和/平均值/最大值/最小值：仅可选数字/日期/货币
#### AI 字段
- 总结（ai_summary）：选择一个或多个字段内容生成智能总结（输出文本）；可设置"自定义总结要求"（≤50 字）
- 翻译（ai_transition）：选择且仅选择 1 个源字段；输出为文本；目标语言仅支持：英文、简体中文、繁体中文、西班牙语、阿拉伯语、印地语、葡萄牙语、孟加拉语、日语、韩语、俄语、德语；可设置"自定义翻译要求"（≤50 字）
- 信息提取（ai_extract）：选择且仅选择 1 个源字段；输出为文本；必须指定"提取信息"；可选"参考示例""自定义提取要求"（≤50 字）
- 分类（ai_classify）：选择且仅选择 1 个源字段；输出为单选
    - 选项来源：AI 自动生成类别；或引用现有单选/多选字段的选项列表（AI 仅能从已有选项中选择）
    - 可选：参考示例、自定义标签要求（≤50 字）
- 智能标签（ai_tag）：选择且仅选择 1 个源字段；输出为多选
    - 选项来源：AI 自动生成标签；或引用现有单选/多选字段的选项列表（AI 仅能从已有选项中选择）
    - 可选：参考示例、自定义标签要求（≤50 字）
- 自定义 AI 自动填充（ai_custom）：配置一条指令（可引用字段或不引用）；输出格式默认文本，可选：文本、数字、单选、多选、日期

### 视图能力与限制
- 多维表格支持表格视图、看板视图、日历视图、画册视图、甘特视图
- 视图由视图名称、视图类型、视图筛选、视图分组、视图排序组成
- 视图名称：字段名称长度必须在 1–100 个字符之间；数据表名 禁止 包含 "[*" 和 "*]"* 特殊字符，若出现上述字符，*必须移除掉这些特殊字符，再进行后续继续处理。
- 视图支持配置字段的显示和隐藏，不支持隐藏索引列
#### 视图筛选能力与限制
- 视图筛选可配置多条，不限制数量
- 多筛选条件需选择"所有满足"或"任一满足"，"所有满足"相当于逻辑"且"，"任一满足"相当于逻辑"或"，禁止单个视图出现"且"、"或"逻辑的组合
- 禁止出现多个筛选条件之间存在重复、冲突或互相排斥的情况
- **视图筛选条件强制约束**：筛选表达式由 筛选字段、比较规则、筛选值 组成
- 筛选字段仅支持当前表字段，禁止选择非当前表字段
- 筛选值仅支持常量值，禁止设置变量
- 当筛选字段类型不同时，比较规则也有不同：
    - 筛选字段为文本、超链接、电话号码、Email、地理位置、条码、总结、翻译、信息提取：支持「为空 / 不为空」判断，无右侧值；支持「等于 / 不等于 / 包含 / 不包含」判断，右侧值为某个具体的字符串常量（不可为变量）
    - 筛选字段位单选、多选：支持「为空 / 不为空」判断，无右侧值；支持「等于 / 不等于 / 包含 / 不包含」已存在的选项常量。若选项不存在，需先在选项列表中补充（若选项为引用项，则需在最原始选项表中添加）
        - 单选字段在「等于 / 不等于」时仅支持单个选项；
        - 单选字段在「包含 / 不包含」时可为多个选项；
        - 多选字段在「等于 / 不等于」时可为多个选项；
        - 多选字段在「包含 / 不包含」时可为多个选项
    - 筛选字段为分类、智能标签：支持「为空 / 不为空」判断，无右侧值；支持「等于 / 不等于 / 包含 / 不包含」已存在的选项常量。若选项不存在，需在选项源中先补充
        - 分类字段在「等于 / 不等于」时仅支持单个类别，「包含 / 不包含」时可为多个类别；
        - 智能标签字段在「等于 / 不等于」时可为多个标签，在「包含 / 不包含」时可为多个标签
    - 筛选字段为人员、创建人、修改人：仅支持「为空 / 不为空」判断，无右侧值
        - 不支持「当前人员」这种变量值，不支持配置筛选条件等于当前用户的视图
    - 筛选字段为附件：支持「为空 / 不为空」判断，无右侧值
    - 筛选字段为日期、创建时间、最后更新时间：支持「为空 / 不为空」判断，无右侧值；支持「等于 / 晚于 / 早于」指定日期（如 '2025/04/13'）；支持「等于 / 晚于 / 早于」今天、明天、昨天；支持「等于」上周、本周、本月、上月、过去七天内、未来七天内、过去30天内、未来30天内
        - **不支持**「早于」上周、本周、本月、上月、过去七天内、未来七天内、过去30天内、未来30天内；
        - **不支持**「不等于」上周、本周、本月、上月、过去七天内、未来七天内、过去30天内、未来30天内；
        - **不支持**「晚于」上周、本周、本月、上月、过去七天内、未来七天内、过去30天内、未来30天内；
        - **不支持**「等于」今天 + X 天；
        - **不支持**「晚于或等于」、「早于或等于」；
    - 筛选字段为数字、自动编号、进度、货币、评分：支持「为空 / 不为空」判断，无右侧值；支持比较「等于 / 不等于 / 大于 / 大于或等于 / 小于或等于 / 小于」指定数值常量
    - 筛选字段为公式：支持「为空 / 不为空」判断，无右侧值；支持比较「等于 / 不等于 / 大于 / 大于或等于 / 小于 / 小于或等于 / 包含 / 不包含」指定常量
    - 筛选字段为复选框：仅支持「等于」判断，右值为「已勾选」或「未勾选」，仅能出现「等于」的条件，如果出现其他判断必须转换（如：筛选复选框字段不为空：要用筛选复选框「等于」「勾选」的记录来描述；筛选复选字段为空：要用筛选复选框「等于」「不勾选」的记录来描述）
    - 筛选字段为查找引用：筛选规则应与所引用字段的类型一致。例如：若引用字段为文本类型，则遵循文本字段的筛选规则；若为数字类型，则遵循数字字段的筛选规则
    - 筛选字段为单向关联、双向关联：支持「为空 / 不为空」判断，无右侧值；支持「等于 / 不等于 / 包含 / 不包含」已存在的常量
        - 仅支持对关联的数据表的索引列的值进行比较
        - 不支持从关联的目标表中引用其他字段
    - 筛选字段为自定义 AI 自动填充：其筛选规则应与所引用字段的类型一致。例如：若引用字段为文本类型，则遵循文本字段的筛选规则；若为数字类型，则遵循数字字段的筛选规则

#### 视图分组能力与限制
- 视图分组仅支持根据某个字段将记录分组展示，不支持在分组的基础上进行进一步的统计分析
- 分组字段仅支持当前表的字段，禁止选择非当前表字段；
- 表格视图最多支持三个分组条件，每个分组字段支持升序或降序排列
- 看板视图只支持一个分组条件，且分组字段仅支持是单选或多选字段

#### 视图排序能力与限制
- 排序字段仅支持当前表的字段，可设置升序或降序排列
- 多排序规则组合逻辑：支持同时设置多个排序字段，按照顺序设定排序优先级
- 排序规则，当排序字段类型不同时，排序规则也有不同，具体排序规则如下
    - 文本、超链接、电话号码、Email、地理位置、条码、总结、翻译、信息提取：
        - 升序：字符串字典顺序，从 A 到 Z
        - 降序：符串字典逆序，从 Z 到 A
    - 单选、多选：
        - 升序：选项值顺序
        - 降序：选项值逆序
    - 数字、自动编号、进度、货币、评分：
        - 升序：数字顺序从小到大
        - 降序：数字顺序从大到小
    - 日期、创建时间、最后更新时间：
        - 升序：时间顺序从早到晚
        - 降序：时间顺序从晚到早

#### 表格视图配置
- 筛选：筛选表达式的值只能为常量或为空；筛选条件必须符合"视图筛选能力与限制"，否则直接拒识
- 排序：按任意字段升序/降序；最多 4 个排序字段
- 分组：按任意字段分组；最多 3 个分组字段；每个分组可设置组内默认排序
- 列隐藏/显示：支持显示、隐藏字段
- 填色：暂不支持（注：进度条颜色属于字段配置，不属于视图填色）
- 不支持的视图配置（涉及时必须拒识）：行高调整、列冻结、列宽调整
#### 看板视图配置
- 筛选、排序同表格视图
- 分组：同表格视图，但是看板视图仅支持 1 个分组字段，要求多个直接拒识
- 卡片配置：仅支持封面设置
- 列隐藏/显示：支持显示、隐藏字段
#### 画册视图配置
- 筛选、排序同表格视图，不支持分组
- 卡片配置：仅支持封面设置
- 列隐藏/显示：支持显示、隐藏字段
#### 日历视图配置
- 筛选同表格视图，不支持分组、排序
- 日程块配置：仅支持标题
- 日历配置：仅支持开始 & 结束日期
- 列隐藏/显示：支持显示、隐藏字段
#### 甘特视图配置
- 筛选、排序、分组同表格视图
- 甘特图配置：仅支持开始 & 结束日期
- 列隐藏/显示：支持显示、隐藏字段
# 注意
- 当用户的 query 涉及类似偏方案选择或经验判断的问题时，由于不属于具体产品能力范围，可予以豁免，回答合理即可。
- 当用户说要快速搜索定位功能信息时，可予以豁免，这里可以用到飞书多维表格中的筛选功能来实现

"""
